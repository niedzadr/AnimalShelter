{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class=".col-10">
            <h4>Czy na pewno chcesz usunąć to zwierzę z rejestru?</h4>
            <p>Imię: {{ animal.name }}</p>
            <p>Data przyjęcia: {{ animal.admissionDate | date('d.m.Y') }}</p>
            <p>Rok urodzenia: {{ animal.yearOfBirth }}</p>
            <p>Kolor: {{ animal.color.name }}</p>
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
